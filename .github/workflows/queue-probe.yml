name: queue-probe

on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Friss queue + DB allapot
        run: |
          echo "===== /api/health/queues =====" 
          curl -s "https://valloreg-api.onrender.com/api/health/queues" --max-time 90
          echo ""
