name: health-body-probe

# IDEIGLENES: a /api/health TELJES JSON-torzset kiirja (db / schema / redis allapot).
on:
  workflow_dispatch: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Health JSON
        run: |
          echo "===== /api/health JSON ====="
          curl -s "https://valloreg-api.onrender.com/api/health" --max-time 90
          echo ""
